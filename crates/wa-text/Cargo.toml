[package]
name = "wa-text"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Shared byte-level text scanners + content hashing for static extraction over WA Web bundle JS. WASM-safe, C-free (sha2 is pure-Rust)."

[dependencies]
sha2 = { version = "0.10", default-features = false }
